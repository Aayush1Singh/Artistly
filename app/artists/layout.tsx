// Metadata for the artist listing page
export const metadata = {
  title: "Browse Artists | Artistly",
  description:
    "Explore and filter a curated list of professional artists by category, location, and price range on Artistly.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
